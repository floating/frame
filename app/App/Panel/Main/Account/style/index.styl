@keyframes arrowSlide
  0%
    opacity 0
    transform translateY(5px)
  20%
    opacity 1
  40%
    opacity 0
    transform translateY(-15px)
  100%
    opacity 0

@keyframes arrowSlideSingle
  0%
    opacity 0
    transform translateY(3px)
  50%
    opacity 1
  100%
    opacity 0
    transform translateY(-3px)

@keyframes headShake
  0%
    transform translateX(0)
  6.5%
    transform translateX(-6px) rotateY(-9deg)
  18.5%
    transform translateX(5px) rotateY(7deg)
  31.5%
    transform translateX(-3px) rotateY(-5deg)
  43.5%
    transform translateX(2px) rotateY(3deg)
  50%
    transform translateX(0)

.headShake
  animation-duration 1s
  animation-fill-mode both
  animation-timing-function ease-in-out
  animation-name headShake
  transform translateZ(0)

.standardFade-enter
  opacity 0

.standardFade-enter.standardFade-enter-active
  opacity 1
  transition var(--standard)

.standardFade-leave
  opacity 1

.standardFade-leave.standardFade-leave-active
  opacity 0
  transition var(--standard)

.signer
  position relative
  padding 0px // 5px 0px 5px
  z-index 3001
  transition var(--standard), 0s linear height
  will-change transform
  transform translate3d(0, 0, 0)
  pointer-events auto
  box-sizing border-box
  z-index 10000000

  // .signerContainer:before
  //   content ''
  //   position absolute
  //   top 0
  //   right 0
  //   left 0
  //   bottom 2px
  //   // background var(--ghostC)
  //   z-index 40
  //   border-radius 12px
  //   z-index 3003

  .signerContainer
    position relative
    z-index 3002
    // border-radius 6px
    // overflow hidden
    // box-shadow 0px 0px 3px -1px rgba(6, 4, 2, 1) // , 0px 1px 1px 0px rgba(6, 54, 122, 0.1), 0 0px 100px -50px rgba(6, 24, 52, .4), cardDrop
    // background var(--ghostB)
    padding-bottom 3px
    // box-shadow 0px 5px 8px -1px var(--thick)

    .accountMain
      position absolute
      top 50px
      left 0
      right 0
      bottom 0
      transform translate3d(0, 0, 0)
      overflow hidden

      .accountsModuleExpand
        background var(--ghostZ)
        position absolute
        top 0px
        left 0
        right 0
        bottom 0
        z-index 2001

        .moduleExpanded
          position absolute
          top 55px
          left 0px
          right 0px
          bottom 45px
          z-index 20000
          border-radius 8px
          box-shadow 0px 0px 12px var(--ghostX)
          
          // .accountModuleInner
          //   overflow-y scroll

      .accountMainScroll
        position absolute
        top 0
        left 0
        right 0
        bottom 0
        overflow-y scroll
        overflow-x hidden
        transform translate3d(0, 0, 0)
        padding-top 13px
        z-index 2000

      .accountMainSlide
        position relative 
        width 100%
        // background var(--ghostC)
        transform translate3d(0, 0, 0)
        transition var(--standardSlow)
        transition-delay 0.32s
        // overflow hidden
        // background red

  .signerAddress
    font-size 10px
    font-family FiraCode, monospace
    letter-spacing 0px
    position absolute
    top 0px
    left 0px
    bottom 0px
    right 0px
    padding-top 0px
    transform translateZ(0)

    .transactionToAddressLargeWrap
      position absolute
      top 14px
      left 61px
      height 40px
      width 140px
      cursor pointer
      z-index 5

    .transactionToAddressLarge
      position absolute
      top 0px
      left 0px
      right 0px
      bottom 0px
      display flex
      font-size 17px
      opacity 1
      pointer-events none

      svg
        color var(--outerspace08)
        padding-top 3px
        padding-left 4px
        padding-right 4px

    .transactionToAddressCopy
      display flex
      justify-content center
      align-items center
      font-size 8px
      letter-spacing 1px
      opacity 1
      pointer-events none
      text-transform uppercase
      font-weight 400
      letter-spacing 1px
      font-family OpenSans, sans-serif
      color var(--outerspace08)
      border-radius 10px
      transform translateY(0px)
      bottom 30px
      opacity 0

    .transactionToAddressFull
      position absolute
      top 2px
      right 0
      bottom 0
      left 0
      display flex
      justify-content center
      align-items center
      font-size 13px
      pointer-events none
      z-index 8

      .transactionToAddressFullCopied
        position absolute
        right 18px
        top 22px

        svg
          position relative
          top -1px
          margin-left 22px

    .transactionToAddressENS
      transform translateY(7px)

      // input
      //   position absolute
      //   top -3px
      //   left -3px
      //   display block
      //   width calc(100% + 6px)
      //   height calc(100% + 6px)
      //   border none
      //   outline none
      //   overflow hidden
      //   opacity 0
      //   cursor default

      // svg
      //   padding-left 5px
      //   padding-bottom 1px
    .transactionToAddressFullHidden
      opacity 0
      // background green


    // .signerAddressActive
    //   .transactionToAddressLarge
    //     opacity 0
    //   .transactionToAddressFull
    //     opacity  1

  .clickGuard
    position absolute
    top 0
    left 0
    bottom 0
    right 0
    z-index 4000
    border-radius 12px

  .signerTop
    position relative
    opacity 1
    z-index 3030000
    border-radius 8px
    transition var(--standard), background 0s linear
    z-index 9999000
    height 63px
    background var(--ghostB)
    margin 0px 5px 0px 5px
    box-shadow 0px 2px 2px var(--ghostZ)

    div
      z-index 3004

    .accountNotificationBadge
      position absolute
      top 15px
      right 60px
      height 33px
      width 33px
      z-index 400000
      border-radius 18px
      display flex
      justify-content center
      align-items center
      font-size 17px
      font-weight 400
      font-family FiraCode, monospace
      color var(--ghostZ)
      transition var(--standard)
      border 1px solid var(--ghostZ)

    .accountNotificationBadgeReady
      color var(--outerspace)
      background var(--ghostC)
      transition var(--standardFast)
      border 1px solid var(--ghostX)

    .accountNotificationBadgeActive
      background var(--good)
      color var(--goodOver)
      border 1px solid transparent
      opacity 1

    .transactionToAddress
      .transactionToAddressLarge
        svg
          color var(--outerspace)

    // .signerNav
    //   position relative
    //   opacity 1
    //   display flex
    //   z-index 3004

    //   // .signerType:hover
    //   //   width 166px
    //   //   margin-left -83px

    .signerSelect
      position absolute 
      // position relative
      top 0px
      right 0px
      // 
      // display flex
      // justify-content center
      // align-items center
      width 90px
      // margin 0px 30px
      height 63px
      // 
      box-sizing border-box
      // 
      z-index 300000
      // background blu
      border-radius 16px
      // border-bottom-right-radius 8px
      
      .signerSelectIconWrap
        width 100%
        height 100%
        transform rotate(0deg)
        box-sizing border-box
        padding 18px 15px 0px 49px
        cursor pointer
        transform translateX(-2px)
        // border-left 1px solid var(--ghostA)

        div, svg
          pointer-events none
        
        .accountActive
          color var(--good)

      .signerSelectIcon
        transition var(--standard)
        pointer-events none

    .signerSelect:hover
      background linear-gradient(90deg, var(--ghostC0), var(--ghostC))

    .accountGrabber
      position absolute
      top 5px 
      bottom 5px
      right 110px
      width 19px
      color var(--ghostX)
      cursor grab
      z-index 5000000000000000
      padding-top 9px
      opacity 1
      transition var(--standardSlow)

      svg
        position relative
        left -8px
        
      svg, div
        pointer-events none

    .accountGrabber:active
      cursor grabbing

    .accountIndicator
      position absolute 
      background var(--ghostY)
      top 16px
      left 6px
      bottom 15px
      width 3px
      border-radius 2px
    
    .accountIndicatorGood
      background var(--good)

    .accountIndicatorLocked
      background var(--moon)

    .signerType
      position absolute 
      top 0px
      left 0px
      width 80px
      height 63px
      // margin 0px 30px
      
      // background red
      // margin-left -55px
      z-index 3400      /// background linear-gradient(var(--ghostB), var(--ghostB))
      // background linear-gradient(var(--ghostB), var(--ghostD))
      // box-shadow 0px 2px 3px var(--thin), 0px 19px 9px -14px var(--thin) !important
      // border-radius 14px
      // border-bottom-right-radius 16px
      transition var(--standard)
      -webkit-app-region no-drag
      box-sizing border-box
      // padding 12px
      z-index 300000

      .signerTypeStatusBadge
        position absolute
        top 13px
        left 10px
        width 18px
        height 18px
        color var(--moon)
        border-radius 50%
        display flex
        justify-content center
        align-items center
        pointer-events none

      .signerTypeStatusBadgeDisconnected
        color var(--outerspace)
        display none

      .signerTypeStatusBadgeLocked
        color var(--moon)
        top 12px
        left 11px

      .signerSelectIconWrap
        // position relative
        // background var(--ghostD)
        
        // width 40px
        // height 39px
        // border-radius 14px
        // transform rotate(0deg)
        position absolute
        top 0
        left 12px
        right 23px
        bottom 0px

        
        // box-sizing border-box
        // padding 10px
        cursor pointer
        // margin-left 6px
        // background red
        // box-shadow 0px 4px 6px -1px var(--thin)
        // display flex
        // padding 19px 9px
        // color var(--good)
        display flex
        justify-content center
        align-items center

        div, svg
          pointer-events none

      // .signerIconHot
      //   color var(--hot)

      .signerIconTrezor
        transform translateY(-3px)

      .signerIconLedger
        transform translateY(-1px)
        

      // .signerIconSmart
      //   color var(--smart)

      // .signerGlow
      //   position absolute
      //   top 0px
      //   left 0px
      //   right 0px
      //   bottom 0px
      //   border-bottom-left-radius 6px
      //   border-bottom-right-radius 6px
      //   overflow hidden

      //   .glow
      //     position absolute
      //     top 0px
      //     left 0px
      //     height 100px
      //     width 100px
      //     margin-left -50px
      //     margin-top -50px
      //     border-radius 50px
      //     background rgba(105, 255, 255, 0.1)
      //     opacity 0
      //     filter blur(30px)

      //   .glowActive
      //     opacity 1

      .signerImage
        position absolute
        top 50%
        left 50%
        // right 0
        height 34px
        width 34px
        margin-top -9px
        margin-left -9px
        opacity 1
        transition var(--standardSlow)

        img
          height 38px

        svg
          color var(--ghostE)
          filter drop-shadow(0px 10px 0px 0px var(--thick)) 

        .trezorImage
          height 35px
          margin-top -6px

      .signerText
        position absolute
        top 44px
        left 0
        right 0
        height 20px
        display flex
        flex-direction column
        justify-content center
        align-items center
        text-transform uppercase
        font-size 11px
        line-height 10px
        font-weight 600
        letter-spacing 2px
        color var(--spacewhite)
        opacity 1
        transition var(--standardSlow)
        margin-left 2px
        // background yellow
        display none

    // .signerTypeDisconnected:hover
    //   margin-left -58px
    //   width 116px

    // .signerTypeDisconnected
    //   position absolute
    //   top 50%
    //   left 50%
    //   margin-top -40px
    //   margin-left -55px
    //   width 80px
    //   height 74px
    //   border-radius 12px
    //   display none
      
    //   // border-bottom-right-radius 6px
    //   // border-top-left-radius 0px
    //   // border-top-right-radius 0px
    //   // display flex
    //   justify-content center
    //   align-items center
    //   overflow hidden
    //   transition .32s cubic-bezier(0.175, 0.885, 0.32, 1.275) all, 0.1s linear top, 0.1s linear bottom, 0.1s linear box-shadow
    //   -webkit-app-region no-drag
    //   // background var(--ghostD)
    //   z-index 15
    //   color var(--outerspace)
    //   transform translateZ(0)
    //   // box-shadow 0px -5px 5px 5px var(--thick), 0px 2px 2px 1px var(--ghostD)

    //   display flex
    //   justify-content center
    //   align-items center
    //   overflow hidden
    //   transition .42s cubic-bezier(0.175, 0.885, 0.32, 1.275) all, 0.1s linear top, 0.1s linear bottom, 0.1s linear box-shadow, 0.1s linear opacity
    //   -webkit-app-region no-drag
    //   box-shadow 0px 2px 2px var(--thick)

    //   .signerDisconnectedTextOpen, .signerDisconnectedTextClose
    //     position absolute
    //     top 50%
    //     left 0
    //     right 0
    //     height 30px
    //     margin-top -15px
    //     // margin-top 40px
    //     display flex
    //     flex-direction column
    //     justify-content center
    //     align-items center
    //     text-transform uppercase
    //     font-size 11px
    //     line-height 10px
    //     font-weight 600
    //     letter-spacing 2px
    //     color var(--outerspaceLight)
    //     transition var(--standardFast)
    //     opacity 0 !important
    //   //
    //   // .signerTypeDisconnectedImageBack
    //   //   color var(--outerspace05) //var(--lowerlight)
    //   //   text-transform uppercase
    //   //   font-size 11px
    //   //   line-height 10px
    //   //   font-weight 300
    //   //   letter-spacing 1px
    //   //   filter drop-shadow(0px 4px 1px var(--doubleThick))
    //   //   position absolute
    //   //   transition var(--standardFast)
    //   //   opacity 0

    //   .signerTypeDisconnectedImageFront
    //     color var(--outerspace08)
    //     text-transform uppercase
    //     font-size 11px
    //     line-height 10px
    //     font-weight 300
    //     letter-spacing 1px
    //     position absolute
    //     margin-top 0px
    //     transition var(--standardFast)
    //     opacity 1
    //     filter drop-shadow(0px 1px 1px var(--thick))

    //   .signerTypeDisconnectedText
    //     text-transform uppercase
    //     position absolute
    //     bottom -40px
    //     font-size 11px
    //     font-weight 600
    //     letter-spacing 1px
    //     margin-left 1px
    //     transform translateY(0px)
    //     color var(--ghostE)
    //     transition var(--standardFast)
    //     text-shadow 0px 1px 1px var(--tripleThick)

    // .signerTypeDisconnected:hover
    //   .signerTypeDisconnectedImageFront
    //     color var(--ghostD)
    // .signerDisconnectedTextOpen
    //   //margin-top -7px
    //   opacity 1 !important
    //   .signerDisconnectedTextClose
    //     //-top -7px
    //     opacity 1 !important
    //   .signerTypeDisconnectedImageBack
    //     ////margin-top -52px
    //     opacity 0
    //   .signerTypeDisconnectedImageFront
    //     //margin-top -52px
    //     opacity 0

    .signerInner
      position absolute
      top -5px
      left 0px
      right 0px
      bottom 2px
      // background var(--outerspaceDark)
      // border-radius 12px
      display flex
      justify-content center
      align-items center
      overflow hidden
      transition .42s cubic-bezier(0.175, 0.885, 0.32, 1.275) all, 0.1s linear top, 0.1s linear bottom, 0.1s linear box-shadow, 0.1s linear opacity
      -webkit-app-region no-drag
      // box-shadow 0px 2px 2px rgba(5, 5, 45, 0.4)
      z-index 25
      transform translateZ(0)

      .signerInset
        position absolute
        top 0px
        left 0px
        right 0px
        bottom 2px
        // background var(--spacewhite) 
        border-radius 12px
        color var(--outerspace)
        // border-top-right-radius 6px
        // border-bottom-left-radius 5px
        // border-bottom-right-radius 5px

    .signerInnerActive
      top 0px
      bottom -2px
      box-shadow 0px 1px 1px rgba(5, 5, 45, 0.4)
      transform translateY(0px)

    .signerInnerDisconnected
      pointer-events none
      opacity 0
      transform translateY(-50px)

    .signerMin
      position absolute
      right 0
      top 0
      bottom 0
      width 40px
      display flex
      justify-content center
      align-items center

    @import './menu'

    .signerStatus
      font-size 16px
      width 100%
      transition var(--standard)
      position absolute
      display flex
      text-align center
      flex-direction column
      // overflow hidden
      font-weight 300

      .signerStatusNotOk
        padding 20px 40px
        display flex
        justify-content center
        align-items center
        flex-direction column

        .signerPinDisplay
          display flex
          justify-content center
          align-items center
          transition var(--standard)
          svg
            margin 0px 5px

        .signerPinMessage
          height 30px
          margin-top 15px
          margin-bottom 15px
          width 140px
          display flex
          justify-content center
          align-items center
          transition var(--standard)
          overflow hidden
          position relative
          border-radius 3px
          box-shadow 0px 2px 3px var(--thick0), 0px 4px 9px var(--thick0) !important

          .signerPinDelete
            position absolute
            display flex
            justify-content center
            align-items center
            top 0
            bottom 0
            width 39px
            left -40px
            transition var(--standard)
            border-right 1px solid var(--thick)

        .signerPinSubmit
          margin-top 0px
          margin-bottom 0px
          background var(--ghostD)
          box-shadow 0px 2px 3px var(--thick), 0px 5px 9px var(--thick) !important
          padding-left 40px
          width 100px

          .signerPinDelete
            left 0px

    .signerAccounts
      position relative
      display flex
      transition var(--standardFast)

    // .signerAccount
    //   // padding 5px 0px 13px 0px
    //   position relative
    //   transition var(--standardFast)
    //   transform translateZ(0)
    //   border-radius 6px
    //   box-sizing border-box

    .signerName
      position absolute
      bottom 12px
      left 62px
      overflow hidden
      display flex
      justify-content center
      align-items center
      transform translateZ(0)
      z-index 3004

      .signerNameText
        position relative
        font-weight 300
        // padding-left 5px
        // padding-bottom 0px
        position relative
        transition var(--standard)
        font-size 12px
        display flex
        justify-content center
        align-items center
        z-index 10
        font-weight 500
        color var(--outerspace08)
        // background red

      .signerNameTextENS
        opacity 0

      .signerNameEdit
        position absolute
        right 0px
        top 10px
        bottom 7px
        width 26px
        border-radius 3px
        display flex
        justify-content center
        align-items center
        color var(--spacewhite)
        transition var(--standard)
        opacity 0
        z-index 9
        background rgba(255, 255, 255, 0)
        box-shadow 0px 0px 0px rgba(5, 5, 45, 0)

        svg
          transition var(--standard)
          color var(--outerspace)

      .signerNameEdit:hover
        transition var(--standard)
        background rgba(255, 255, 255, 0.4)
        box-shadow 0px 1px 1px rgba(5, 5, 45, 0.2)

    .signerNameSettings
      transition var(--standard)
      .signerNameText
        padding-right 35px
      .signerNameEdit
        transition var(--standard)
        opacity 1

    .signerInfo
      position relative
      font-size 16px
      letter-spacing 0px
      position relative
      height 0px
      padding-top 0px
      text-align center
      transform translateZ(0)
      transition var(--standard)
      opacity 0
      margin-top 2px
      display none

    .addressSelect
      width 100%
      height 31px
      border-radius 6px
      position absolute
      bottom 0
      z-index 5
      transform translateZ(0px)
      display none

      .addressSelectButton
        width 100%
        height 30px
        min-height 30px
        border-radius 6px
        font-size 11px
        text-transform uppercase
        font-weight 600
        display flex
        justify-content center
        align-items center
        box-sizing border-box
        color var(--outerspace)
        letter-spacing 1px

        .addressSelectArrow
          // margin-top 5px
          transform translateZ(0)
          margin 1px 8px 0px 7px
          transition var(--standardFast)
          will-change transform
          svg
            transition var(--standardSlow)

  // .signerTopActive
  //   background var(--ghostC)

  .accountListWrap
    position absolute
    top 100px
    left 0
    right 0
    // height 0px
    height 0px
    background var(--ghostB)
    // height 40px
    z-index 3015
    transition var(--standardFast)
    border-bottom-left-radius 16px
    border-bottom-right-radius 16px
    overflow hidden
    box-shadow 0px 2px 3px var(--thin), 0px 19px 9px -14px var(--thin) !important
    transform translateZ(0)
    display none

    .accountList
      position absolute
      left 0
      right 0
      bottom 0
      height 330px
      z-index 3018
      overflow hidden
      padding 15px 0px 10px 0px

      .settingsMenu
        background linear-gradient(var(--ghostB), var(--ghostD))
        display flex
        justify-content center
        flex-direction column
        align-items center
        position absolute
        left 0
        right 0
        bottom 0
        height 50px
        transition var(--standardSlow)
        opacity 1
        pointer-events auto
        .settingsMenuSelect
          position relative
          height 12px
          width 105px
          box-sizing border-box
          .settingsMenuMark
            height 12px
            position absolute
            transition var(--standard)
            .settingsMenuMarkLine
              position absolute
              background outerspace
              height 2px
              top 0px
              left 7px
              right 7px
              border-radius 1px
        .settingsMenuItems
          box-sizing border-box
          height 33px
          width 105px
          display flex
          .settingsMenuItem
            position relative
            box-sizing border-box
            z-index 1230
            width 35px
            height 33px
            color var(--outerspace08)
            .settingsMenuItemIcon
              top 0
              right 0
              bottom 0px
              left 0
              position absolute
              z-index 1234
              padding 10px 10px
          .settingsMenuItemSelected, .settingsMenuItem:hover
            color var(--outerspace)

      .accountListItems
        background var(--ghostB)
        height 280px
        min-height 280px
        width 100%
        display flex
        justify-content flex-start
        align-items center
        flex-direction column
        position relative
        z-index 3
        transition var(--standardSlow)
        // opacity 0
        transform translateY(-200px)
        pointer-events none

        .accountListItem
          background var(--ghostB)
          width calc(100% - 40px)
          height 55px
          min-height 55px
          border-bottom 1px solid var(--thin)
          display flex
          justify-content space-between
          align-items center
          padding 0px 20px 0px 20px
          font-family FiraCode, monospace
          color var(--outerspace08)
          position relative
          z-index 4

          .accountListItemCheck
            color var(--outerspace01)
            margin-bottom 3px

          .accountListItemAddress
            svg
              margin 0px 4px 0px 4px

        .accountListItem:last-child
          border-bottom none

        .accountListItemSelected
          background var(--ghostC)
          color var(--outerspace)
          border-radius 6px
          border-bottom 1px solid var(--thick0)
          box-shadow 0px 2px 3px var(--thin), 0px 19px 9px -14px var(--thin) !important
          position relative
          z-index 10
          .accountListItemCheck
            color var(--outerspace)

      .accountPageToggle
        padding-top 20px
        height 20px
        min-height 20px
        padding-bottom 10px
        width 100%
        display flex
        justify-content center
        align-items center
        font-family FiraCode, monospace
        position relative
        z-index 2
        transition var(--standardSlow)
        opacity 0
        pointer-events none
        transform translateY(-200px)

        .accountPageCurrent
          border-radius 3px
          height 27px
          width 27px
          display flex
          justify-content center
          align-items center
          font-size 18px

        .accountPageButton
          border-radius 3px
          height 27px
          width 27px
          display flex
          justify-content center
          align-items center
          box-sizing border-box

        .accountPageButtonLeft
          padding-right 1px

        .accountPageButtonRight
          padding-left 1px

        .accountPageButton:hover
          background var(--ghostD)

  .accountMenu
    position absolute
    top 64px
    right 0px
    left 0px
    // padding-top 20px
    border-radius 6px
    z-index 3005
    overflow-x hidden
    overflow-y hidden
    // opacity 1
    transition var(--standard), 0.1s linear background
    will-change transform
    background var(--ghostC)
    z-index 9999003
    height 40px
    background var(--ghostC)
    //animation-timing-function: cubic-bezier(0.1, -0.6, 0.2, 0)
    // background red // var(--ghostB)
    box-shadow 0px 4px 6px -1px var(--thick)
    animation-delay 0.2s
    .accountMenuLeft
      position absolute
      top 0px
      left 0px
      bottom 0px
      display flex
      .accountMenuItem
        border-right 1px solid var(--thick)
      .accountMenuItem:first-child
        padding-left 4px

    .accountMenuRight
      position absolute
      top 0px
      right 0px
      bottom 0px
      .accountMenuItem
        border-left 1px solid var(--thick)
      .accountMenuItem:last-child
        padding-right 4px
    
    .accountMenuItem
      height 40px
      width 40px
      display flex
      justify-content center
      align-items center
      cursor pointer

      div, svg 
        pointer-events none

    .accountMenuItem:hover
      background var(--ghostD)

  .signerMid
    position absolute
    top calc(22px + 83px)
    right 0px
    bottom 7px
    left 0px
    // padding-top 20px
    border-radius 6px
    z-index 3005
    overflow-x hidden
    overflow-y hidden
    opacity 1
    transition var(--standard), 0.1s linear background
    will-change transform
    background var(--ghostC)
    z-index 9999003
    //animation-timing-function: cubic-bezier(0.1, -0.6, 0.2, 0)
    // background red // var(--ghostB)
    box-shadow 0px 4px 6px -1px var(--thick)
    animation-delay 0.4s

    > div
      transition var(--standard)
      opacity 0

    .signerMidMenu
      background var(--thin)
      height 50px
      display flex
      flex-direction column
      justify-content center
      align-items center

      .signerMenuItems
        height 25px
        display flex
        justify-content center
        align-items center
        margin-top 5px

        .signerMenuItem
          height 30px
          width 40px
          display flex
          justify-content center
          align-items center

      .signerMenuSelect
        height 10px
        width 80px
        position relative

        .signerMenuSelectMarker
          position absolute
          left 0px
          height 2px
          width 20px
          // margin 0px 4px
          margin 0px 10px
          background black

    .signerNode
      display flex
      justify-content center
      align-items center
      font-size 12px
      height 40px
      border-top 1px solid var(--ghostB)

  .signerBot
    position absolute
    bottom 3px
    left 3px
    right 3px
    opacity 1
    z-index 3020
    height 64px
    // border-bottom-left-radius 16px
    // border-bottom-right-radius 16px
    pointer-events none
    transition var(--standard)
    transform translateZ(0)
    // background linear-gradient(rgba(255, 255, 255, 0), var(--ghostD), var(--ghostD))
    background var(--ghostC)
    overflow hidden

  .signerEvents
    height 200px
    overflow scroll
    background rgba(0, 0, 50, 0.1)
    width 100%

  .signerSelectArrowWrap
    position absolute
    display flex
    justify-content center
    align-items center
    font-size 14px
    font-weight 200
    text-align center
    top 0
    bottom 0px
    width 16px
    z-index 1
    color var(--spacewhite)
    flex-direction column
    opacity 1
    transition var(--standard)
    transition-delay 0.64s
    
    .signerSelectArrows
      height 50px
      width 16px
      // border-radius 25px
      position relative
      
      .signerSelectArrow
        position absolute
        top 0
        left 0
        right 0
        animation-duration 2.4s
        animation-name arrowSlide
        animation-iteration-count infinite
        animation-fill-mode none
        animation-play-state running
        transform translateZ(0)
        will-change transform, opacity
        opacity 0

        svg
          color var(--outerspace)
          opacity 1
          transform translateZ(0)
          will-change transform, opacity

      .signerSelectArrow:first-child
        animation-delay 0s
        margin-top 12px

      .signerSelectArrow:nth-child(2)
        animation-delay 0.09s
        margin-top 19px

      .signerSelectArrow:nth-child(3)
        animation-delay 0.18s
        margin-top 26px

      .signerSelectArrow:nth-child(4)
        animation-delay 0.27s
        margin-top 33px

      .signerSelectArrow:nth-child(5)
        animation-delay 0.36s
        margin-top 40px

  .signerSelectDown
    transform rotate(180deg)
    .signerSelectArrows
      .signerSelectArrow
        svg
          color var(--spacewhite)

  .signerSelectLeft
    left 3px

  .signerSelectRight
    right 3px

  .signerSelectDown.signerSelectLeft
    left -50px
    opacity 0

  .signerSelectDown.signerSelectRight
    right -50px
    opacity 0

  .signerReady
    font-size 12px
    width 100%

// .trezorPhraseInput
//   height 200px
//   width 100%
//   background var(--outerspace05)
//   position relative
//   display flex
//   justify-content center
//   transition var(--standard)
//   padding 20px 0px 20px 0px
//   height 120px
//   overflow hidden

// .trezorPinWrap:before
//   content ''
//   position absolute
//   top 0
//   right 0
//   bottom 3px
//   left 0
//   background #212d2e
//   border-radius 3px

// .trezorPinWrap
//   width 100%
//   background var(--outerspace)
//   position relative
//   display flex
//   justify-content center
//   transition var(--standard)
//   padding 20px 0px 20px 0px
//   height 120px
//   overflow hidden
//   background blue

//   .trezorPinInput
//     display flex
//     width 120px
//     height 120px
//     flex-wrap wrap-reverse
//     z-index 1000

//     .trezorPinInputButton
//       min-width 40px
//       min-height 40px
//       display flex
//       justify-content center
//       align-items center

//       svg
//         color var(--spacewhite)

//     .trezorPinInputButton:hover
//       svg
//         transform scale(1.7)

//     .trezorPinInputButton:active
//       svg
//         transform scale(2.1)
//         transition none

// .okSigner
//   .signerTop
//     .signerNav
    // .signerTypeDisconnected
    //   .signerDisconnectedTextOpen
    //     opacity 1 !important
    //   .signerDisconnectedTextClose
    //     opacity 0 !important
      // .signerType
      //   width 160px
      //   // margin-left -80px
      //   .signerSelect
      //     opacity 1
      //   .signerInner
      //     left 25px
      //     right 25px
      //   .signerTypeSettings
      //     transform translateY(-30px)

.openSigner
  // padding 0px 5px 5px 5px
  .signerMid
    > div
      opacity 1
  .signerTop
    .signerStatus
      .signerInfo
        opacity 1
        height 20px
    // .signerNav
    //   .signerTypeDisconnected
    //     width 160px
    //     // margin-left -80px
    //     // .signerDisconnectedTextOpen
    //     //   opacity 0
    //     // .signerDisconnectedTextClose
    //     //   opacity 1
    //   .signerType
    //     .signerSelect
    //       opacity 0
    //     .signerInner
    //       left 0px
    //       right 0px
    //       .low
    //         margin-left -20px

  .signerSelectDown.signerSelectRight
    right 0

  .signerSelectDown.signerSelectLeft
    left 0

  .accountListWrap
    height 60px

.signerInSettings
  .signerTop
    .addressSelect
      transform translateY(0px)
      pointer-events auto
  .accountListWrap
    height 130px
    transform translateY(0px)
    will-change height

.signerAccountExpand
  .addressSelectArrow:first-child
    transition var(--standardSlow)
    svg
      transform rotate(-180deg)
  .addressSelectArrow:last-child
    transition var(--standardSlow)
    svg
      transform rotate(180deg)
  .accountListWrap
    transition var(--standardSlow)
    height 425px
    .accountList
      .accountListItems
        pointer-events auto
        transform translateY(0px)
        // opacity 1
      .accountPageToggle
        opacity 1
        pointer-events auto
        transform translateY(0px)
      .settingsMenu
        opacity 0
        pointer-events none
        transform translateY(50px)

.accountModule
  position absolute 
  top 0
  left 5px
  right 5px
  // border-bottom 1px solid var(--ghostA)
  // background var(--ghostC)
  transform translate3d(0, 0, 0)
  transition var(--standardFast) // , 0s linear height // 0.05s linear allheight // // , height 1s linear
  // box-shadow 0px 10px 10px var(--ghostA)
  // padding 0px 0px 0px 0px
  // overflow hidden

.accountModuleInner
  position absolute
  top 0
  left 0px
  right 0px
  bottom 0px
  // margin 2px
  border-radius 10px
  background var(--ghostB)
  // box-shadow 0px 2px 2px var(--ghostX)
  box-shadow 0px 2px 6px var(--ghostZ)
  transform translate3d(0, 0, 0)
  // transition 0.05s linear all // var(--standard)
  // margin-bottom 2px
  overflow hidden

  // .accountModuleContent
  //   padding 2px 2px 5px 2px

  // div
  //   padding 0px 0px
  // border-bottom 1px solid var(--ghostA)

  .moduleHeader:before
    content ''
    position absolute
    bottom -2px
    right 16px
    left 16px
    height 1px
    background var(--ghostA)
    
  .moduleHeader
    position relative
    font-size 11px
    text-transform uppercase
    font-weight bold
    letter-spacing 2px
    padding 8px 
    height 30px
    text-align center
    box-sizing border-box

    .moduleHeaderClose
      background var(--moon)
      color var(--moonOver)
      width 20px
      height 20px
      border-radius 10px
      position absolute
      right 5px
      top 5px
      bottom 0
      display flex
      justify-content center
      align-items center
      cursor pointer
      *
        pointer-events none

    .moduleHeaderLoading
      position absolute
      left 6px
      top 1px
      bottom 0
      // padding 0px 10px
      display flex
      justify-content center
      align-items center
      
    .moduleHeaderLoadingLoader
      border-radius 50%
      width 12px
      height 12px
      margin 12px auto
      font-size 10px
      position relative
      text-indent -9999em
      border-top 3px solid var(--ghostZ)
      border-right 3px solid var(--ghostZ)
      border-bottom 3px solid var(--ghostZ)
      border-left 3px solid var(--good)
      transform translateZ(0)
      animation load 0.5s infinite linear

  .moduleHeader:before
    background none

  .moduleHeaderBorderless
    border-bottom none

  .moduleComingSoon
    padding 14px 0px 20px 0px
    display flex
    justify-content center
    color var(--ghostZ)
    font-weight 100
    font-size 24px

  //.moduleButtonBad:hover
    // border 1px solid var(--bad)
    /// background var(--bad01)

  .moduleMain
    padding 16px

  .moduleFooter
    padding 16px

  .moduleMainPermissions
    padding 0px 16px 0px 16px
    box-sizing border-box

  .moduleMainSettings
    font-size 11px
    text-transform uppercase
    font-weight 400

  // .moduleMid
  //   padding-bottom 42px

  .moduleFooterAction
    position absolute
    bottom 0px
    width 100%
    cursor pointer
    text-align center
    font-size 12px
    text-transform uppercase
    border-top 1px solid var(--ghostA)
    box-sizing border-box
    font-weight 500
    letter-spacing 1px
    padding 12px 
    height 42px
    text-align center
    box-sizing border-box
    background var(--ghostB)
    z-index 400000
    
  .moduleFooterAction:hover
    background var(--ghostD)


    // .moduleMain
    //   padding 8px

.transparentModule
  .accountModuleInner
    position absolute
    padding-right 20px
    top 0
    left 0px
    right -20px
    bottom 0px
    border-radius 0px
    background transparent
    overflow-y scroll
    overflow-x hidden
    box-shadow none
    transform translate3d(0, 0, 0)
    // background blue

.signerVerifyText
  font-size 15px
  text-align center
  display flex
  justify-content center
  align-items center
  height 70px
  padding 0px 30px
  
.signerVerifyResponse
  background var(--bad)
  border-radius 8px
  color var(--badOver)
  font-size 16px
  padding 20px
  font-weight 400
  display flex
  justify-content center
  text-align center
  position relative
  box-sizing border-box
  width calc(100% - 20px)
  margin 0px 10px 28px 10px

.signerVerifyResponseSuccess
  background var(--good)
  color var(--goodOver)

.moduleRow
  height 30px
  margin-bottom 4px
  display flex
  align-items center

  input
    z-index 200
    user-select auto
    background var(--ghostC)
    outline none
    border none
    margin-left 8px
    height 100%
    color var(--outerspace)
    border-radius 8px
    padding 0px 8px

.moduleButton
  width 100%
  height 40px
  background var(--ghostC)
  border-radius 8px
  color var(--outerspace)
  display flex
  justify-content center
  align-items center
  font-weight 400
  font-size 13px
  cursor pointer
  box-sizing border-box
  text-transform uppercase
  box-shadow 0px 2px 2px var(--ghostA)
  
  svg
    position relative

  span
    padding-left 4px

  *
    pointer-events none

.moduleButton:hover
  background var(--ghostD)
  box-shadow 0px 4px 6px var(--ghostA)

.moduleButtonBad
  color var(--bad)

.moduleButtonGood
  color var(--good)

.moduleButtonLocked
  color var(--ghostG)

.moduleButtonLocked:hover
  background var(--ghostB)
  box-shadow 0px 2px 2px var(--ghostA)


@import '../Balances/style'
@import '../Gas/style'
@import '../Inventory/style'
// @import '../Settings/style'
@import '../Requests/style'
@import '../Launcher/style'
@import '../SignerStatus/style'
