@keyframes bounceIn {
  from,
  20%,
  40%,
  60%,
  80%,
  to {
    animation-timing-function cubic-bezier(0.215, 0.61, 0.355, 1)
  }

  0% {
    opacity 0
    transform scale3d(0.3, 0.3, 0.3)
  }

  20% {
    transform scale3d(1.1, 1.1, 1.1)
  }

  40% {
    transform scale3d(0.9, 0.9, 0.9)
  }

  60% {
    opacity 1
    transform scale3d(1.03, 1.03, 1.03)
  }

  80% {
    transform scale3d(0.97, 0.97, 0.97)
  }

  to {
    opacity 1
    transform scale3d(1, 1, 1)
  }
}
.bounceIn {
  animation-duration 0.75s
  animation-name bounceIn
}

.scaleIn
  animation scalein 600ms cubic !important

.loader,
.loader:after {
  border-radius 100%
  width 7em
  height 7em
}
.loader {
  font-size 7px
  position relative
  text-indent -9999em
  border-top 6px solid var(--outerspace01)
  border-right 6px solid var(--outerspace01)
  border-bottom 6px solid var(--outerspace01)
  border-left 6px solid var(--outerspace08)
  transform translateZ(0)
  animation load8 1.1s infinite linear !important
}
@keyframes load8 {
  0% {
    transform rotate(0deg)
  }
  100% {
    transform rotate(360deg)
  }
}

@keyframes shine {
  0% {
    transform translateY(-2000px)
  }
  100% {
    transform translateY(0)
  }
}

@keyframes sk-scaleout
  0%
    transform scale(0)
  100%
    transform scale(1.0)
    opacity 0

@keyframes scalein
  0%
    transform scale(0.8)
    opacity 0.2
  100%
    transform scale(1)
    opacity 1

@keyframes spin
  0%
    transform rotate(45deg)
  100%
    transform rotate(405deg)

@keyframes sheen
  0%
    transform translateX(-300px) rotate(-30deg)
  80%
    transform translateX(-300px) rotate(-30deg)
  95%
    transform translateX(300px) rotate(30deg)
  100%
    transform translateX(-300px) rotate(60deg)

@keyframes pulse
  0%
    fill var(--outerspace)
  50%
    fill var(--outerspacePulse)
  100%
    fill var(--outerspace)

@keyframes bars
  0%
    opacity 0
  30%
    opacity 1
  60%
    opacity 0

.txProgressNoticeBar-10
  animation bars 3000ms infinite linear 0s
.txProgressNoticeBar-9
  animation bars 3000ms infinite linear 0.1s
.txProgressNoticeBar-8
  animation bars 3000ms infinite linear 0.2s
.txProgressNoticeBar-7
  animation bars 3000ms infinite linear 0.3s
.txProgressNoticeBar-6
  animation bars 3000ms infinite linear 0.4s
.txProgressNoticeBar-5
  animation bars 3000ms infinite linear 0.5s
.txProgressNoticeBar-4
  animation bars 3000ms infinite linear 0.6s
.txProgressNoticeBar-3
  animation bars 3000ms infinite linear 0.7s
.txProgressNoticeBar-2
  animation bars 3000ms infinite linear 0.8s
.txProgressNoticeBar-1
  animation bars 3000ms infinite linear 0.9s
.txProgressNoticeBar-0
  animation bars 3000ms infinite linear 1s

.spinner
  width 100px
  height 100px
  margin 100px auto
  background-color var(--outerspace08)
  border-radius 100%
  animation sk-scaleout 600ms infinite cubic

.signSuccess
  width 100px
  height 100px
  margin 100px auto
  background-color var(--outerspace08)
  border-radius 100%
  animation sk-scaleout 600ms infinite cubic

.slideUp-enter
  opacity 0
  margin-top -200px
  transition standardSlow

.slideUp-enter.slideUp-enter-active
  opacity 1
  margin-top 0px
  transition standardSlow
  transition-delay 0.32s

.slideUp-leave
  opacity 1
  transition standardSlow

.slideUp-leave.slideUp-leave-active
  opacity 0
  margin-top -200px

.txProgressNotice
  position absolute
  width 100%
  top 50%
  height 1000px
  margin-top -500px

  .txProgressNoticeBars
    position absolute
    top 50%
    margin-top -8px
    left 8px
    right 8px
    height 16px
    display flex
    justify-content space-between
    align-items center
    transition standard

    .txProgressNoticeBarDeadzone
      width 17px
      height 19px

    .txProgressNoticeBar
      opacity 0
      width 4px
      border-radius 2px
      height 15px
      background var(--thick)

  .txProgressNoticeIcon
    position absolute
    top 50%
    left 0
    right 0
    margin-top -80px
    height 160px
    width 100%
    display flex
    justify-content center
    align-items center
    z-index 1000

  .txProgressCancel
    position absolute
    top 50%
    left 100px
    right 100px
    margin-top 50px
    height 20px
    border-radius 12px
    display flex
    justify-content center
    align-items center
    z-index 1001
    font-weight 500
    background var(--thinner)

  .txProgressCancel:hover
    background var(--thin)

  .txProgressNoticeText
    position absolute
    top 50%
    left 0
    right 0
    margin-top 20px
    height 20px
    width 100%
    display flex
    justify-content center
    align-items center
    z-index 1001
    font-weight 500

  .txProgressNoticeLoader
    height 100px
    width 100px
    margin-bottom 20px
    position relative

    .txProgressNoticeLoaderIcon
      position absolute
      top 0
      left 0
      height 100px
      width 100px
      z-index 1000004

    .txProgressNoticeLoaderBars
      position absolute
      top 0
      right 0
      height 50px
      width 50px

.signerRequests
  position absolute
  top 10px
  right 0
  bottom 0
  left 0
  z-index 200
  padding 0px 0px 64px 0px
  transition standardSlow
  overflow-y scroll
  overflow-x hidden
  transform translateZ(0)

  .requestTitle:before
    content ''
    position absolute
    bottom -2px
    right 10px
    left 10px
    height 4px
    border-radius 2px
    background var(--thin)

  .requestTitle
    position relative
    display flex
    align-items flex-end
    justify-content space-between
    height 40px
    font-size 22px
    padding 0px 10px 10px 10px

    .requestCount
      background var(--thin)
      width 50px
      height 30px
      border-radius 15px
      display flex
      justify-content center
      align-items center
      font-weight 300

  .requestBottom
    position relative
    display flex
    align-items center
    justify-content center
    height 40px
    padding-left 10px
    padding-right 10px

    svg
      color var(--outerspace08)

  .requestContainerWrap
    display flex
    justify-content center
    align-items center
    flex-direction column
    position relative
    tranform translateZ(0)

   .requestContainer
     width calc(100% - 20px)
     position relative
     display flex
     flex-direction column
     transition standard
     transition-delay 1s
     tranform translateZ(0)

    .noRequests
      position absolute
      top 0px
      left 0
      right 0
      height 155px
      font-size 12px
      font-weight 400
      letter-spacing 1px
      text-transform uppercase
      display flex
      justify-content center
      align-items center
      transition standard
      tranform translateZ(0)
      color var(--doubleThick)

    .recentRequests
      position absolute
      top 1px
      left 3px
      right 0px
      height 20px
      pointer-events none
      justify-content space-between
      align-items center
      display flex
      text-transform uppercase
      font-size 11px
      line-height 10px
      font-weight 600
      letter-spacing 2px
      transition standard
      color var(--outerspace08)

  .signerRequestItems
    overflow scroll

  default = 0px 3px 2px 1px var(--thin), 0px 25px 20px -15px var(--thick)

  .signerRequest
    position absolute
    top 0px
    left 0px
    right 0px
    padding-bottom 10px
    justify-content space-between
    box-shadow 0px 2px 0px var(--thick), 0px 8px 8px -2px var(--thick), 0px 0px 10px -2px var(--thick)
    border-radius 12px
    height 380px
    display flex
    justify-content center
    transition standard
    background var(--ghostC)
    z-index 2000
    color var(--outerspace08)
    tranform translateZ(0)

  .requestOverlay
    display none
    position absolute
    top 0px
    left 0px
    right 0px
    bottom 0px
    z-index 9999999999999
    border-radius 12px
    pointer-events none
    overflow hidden
    .requestSheen
      position absolute
      z-index 30000002
      top -400%
      height 1000%
      width 90px
      animation sheen 12s infinite ease
      background linear-gradient(90deg, rgba(247, 251, 255, 0), rgba(237, 241, 255, 0.7), rgba(247, 251, 255, 0))
      background-blend-mode multiply

  .signerRequestSuccess
    align-items center
    .requestApprove
      opacity 0
      pointer-events none

  .signerRequestPending
    align-items center
    svg
      color var(--outerspace08)
    .requestApprove
      opacity 0
      pointer-events none

  .signerRequestError
    align-items center
    .requestApprove
      opacity 0
      pointer-events none

  .signerRequestDeclined
    align-items center
    .requestApprove
      opacity 0
      pointer-events none

  .signerRequestMonitor
    align-items center
    background var(--ghostD)
    svg
      color var(--outerspace08)
    .approveTransactionPayload
      color var(--outerspace08)
    .requestApprove
      opacity 0
      pointer-events none

  .requestNotice
    height 100%
    width 100%
    transform translateZ(0px)
    background linear-gradient(var(--ghostD), var(--ghostB), var(--ghostD))
    border-radius 12px
    overflow hidden

    .requestNoticeInner
      position absolute
      top 0px
      right 0
      bottom 0px
      left 0
      display flex
      justify-content center
      align-items center
      flex-direction column

      .requestNoticeInnerText
        padding-top 30px
        font-size 12px
        font-weight 400
        text-transform uppercase
        letter-spacing 2px
        white-space normal
        width 280px
        text-align center

      .cancelRequest
        margin-top 30px
        font-size 10px
        background var(--thin)
        height 20px
        padding 0px 20px 0px 22px
        border-radius 12px
        letter-spacing 2px
        font-weight 500
        display flex
        justify-content center
        align-items center
        text-transform uppercase

      .cancelRequest:hover
        background var(--thick)

  .requestProvider
    position absolute
    top 60px
    right 0
    bottom 60px
    left 0
    display flex
    justify-content center
    align-items center
    flex-direction column
    transform translateZ(0px)

    .requestProviderOrigin
      font-size 22px
      font-weight 200
      width 300px
      text-align center
      transform translateZ(0px)
      direction rtl
      text-overflow ellipsis
      white-space nowrap
      overflow hidden

    .requestProviderOrigin18
      font-size 17
      font-weight 300

    .requestProviderOrigin12
      font-weight 300
      font-size 14

    .requestProviderSub
      font-size 12px
      font-weight 400
      letter-spacing 2px
      text-transform uppercase
      display flex
      justify-content center
      align-items center
      margin-top 10px
      transform translateZ(0px)
      letter-spacing 1px

  .approveTransaction
    width 100%

    .approveTransactionIcon
      width 100%
      padding-top 10px
      padding-bottom 10px
      display flex
      justify-content center
      display none

      svg
        color var(--outerspace08)

    .approveRequestHeader
      position absolute
      top 0
      left 0
      right 0
      height 60px
      display flex
      justify-content center
      align-items center
      font-weight 400
      font-size 14px
      background var(--ghostD)
      color var(--outerspace08)
      border-top-left-radius 16px
      border-top-right-radius 16px
      box-shadow 0px 1px 0px var(--thick)
      text-transform uppercase

      .approveRequestHeaderTitle
        text-align center
        letter-spacing 2px

      .approveRequestHeaderOrigin
        font-size 10px
        letter-spacing 0px
        text-transform none

      .approveRequestHeaderTag
        position absolute
        bottom -8px
        left 80px
        right 80px
        height 16px
        border-radius 8px
        box-sizing border-box
        padding-top 1px
        font-size 10px
        text-align center
        font-weight 500
        background var(--good)
        color var(--spacewhite)

      .approveRequestHeaderTagInvalid
        background var(--bad)

      .approveRequestHeaderIcon
        width 40px
        height 60px
        display flex
        justify-content center
        align-items center
        z-index 10
        transition standard
        position absolute
        border-radius 2px
        left 5px
        color var(--outerspace08)

      .approveRequestHeaderNonce
        width 80px
        height 60px
        display flex
        justify-content flex-end
        align-items center
        z-index 10
        transition standard
        position absolute
        border-radius 2px
        right 10px
        color var(--outerspace08)

      .approveRequestHeaderClose
        width 60px
        display flex
        justify-content center
        align-items center
        z-index 10
        transition standard
        opacity 0

    .approveTransactionPayload
      position absolute
      display flex
      align-items center
      flex-direction column
      top 0
      left 0
      right 0
      bottom 0

  .signValue
    position absolute
    top 57px
    left 0
    right 0
    bottom 60px
    margin 0
    font-size 20px
    font-weight 300
    overflow-y scroll
    overflow-x hidden

    .signValueInner
      width 100%
      display flex
      justify-content space-around
      align-items center
      flex-direction column
      font-size 20px
      padding 20px
      min-height 100%
      box-sizing border-box
      word-break break-all

  .signTypedData
    position absolute
    top 57px
    left 0
    right 0
    bottom 60px
    margin 0
    font-size 20px
    font-weight 300
    overflow-y scroll
    overflow-x hidden

    .signTypedDataInner
      width 100%
      font-size 20px
      padding 20px
      min-height 100%
      box-sizing border-box

      .signTypedDataSection
        margin-bottom 40px

      .signTypedDataTitle
        display block
        font-size 16px
        margin-bottom 5px
        font-weight bold

  .transactionValue
    position absolute
    top 57px
    left 0
    right 0
    height 55px
    display flex
    justify-content space-around
    align-items center
    flex-direction column
    font-size 20px
    padding 15px 10px 15px 10px
    font-weight 300

  .transactionSubtitle
    text-transform uppercase
    font-size 13px
    font-weight 400
    letter-spacing 1px

  .transactionSymbol
    display flex
    justify-content center
    align-items center
    font-size 16px
    font-weight bold
    font-family FiraCode, sans-serif
    margin 2px 8px 0px 0px 
    padding 0px 16px
    background var(--ghostE)
    border-radius 12px
    height 25px
    min-height 25px
    max-height 25px
      
  .transactionTotals
    display flex
    justify-content center
    align-items center
    font-size 28px

  .transactionTotalETH
    display flex
    justify-content center
    align-items center

  .networkFeeSymbol
    position relative
    font-size 12px
    font-weight 600
    font-family FiraCode, sans-serif
    left -2px
    top 1px

  .transactionTotalUSD
    display flex
    justify-content center
    align-items center
    font-size 16px
    font-weight bold
    font-family FiraCode, sans-serif
    margin 2px 0px 0px 8px 
    padding 0px 16px
    background var(--ghostE)
    border-radius 12px
    height 24px
    min-height 24px
    max-height 24px

  .transactionWarning
    color var(--bad)

  .transactionFeeWarning
    color var(--bad)
    position absolute
    top 0
    left 0
    right 0
    height 20px
    display flex
    justify-content center
    align-items center

  .transactionFee
    position absolute
    top 120px
    left 0
    right 0
    height 45px
    display flex
    justify-content space-around
    align-items center
    flex-direction column
    padding 15px 10px 10px 10px
    font-weight 300

    .transactionTotals
      font-size 20px

    .transactionTotalUSD
      font-size 15px
      padding 2px 8px
      font-weight 300

  .transactionData
    position absolute
    left 10px
    right 10px
    top 171px
    height 32px
    z-index 2000
    display flex
    justify-content center
    align-items center
    color var(--outerspace08)
    font-size 14px
    font-weight 400
    letter-spacing 1px
    text-transform uppercase
    transition standard

    .transactionDataHeader
      position absolute
      top 0
      left 0
      right 0
      height 32px
      border-radius 12px
      justify-content space-between
      display flex
      align-items center
      text-transform uppercase
      letter-spacing 1px
      font-weight 400
      z-index 10
      background var(--ghostD)
      box-shadow 0px 2px 0px var(--thick), 0px 8px 8px -2px var(--thick), 0px 0px 10px -2px var(--thick)

      .transactionDataLabel
        display flex
        justify-content center
        align-items center
        z-index 10
        transition standard

      .transactionDataNotice
        width 32px
        display flex
        justify-content center
        align-items center
        z-index 10
        transition standard
        color var(--outerspace08)

      .transactionDataIndicator
        width 21px
        height 21px
        max-width 21px
        max-height 21px
        z-index 10
        transform rotate(-180deg)
        transition standardSlow
        border 2px solid var(--thick)
        border-radius 12px
        margin 4px

        svg
          position relative
          top 3px
          left 5px

  .transactionDataBody
    position absolute
    color var(--outerspace08)
    top 35px
    left 0
    right 0
    bottom 0
    display flex
    padding 16px 14px
    user-select all
    overflow hidden
    z-index 90
    background var(--ghostD)
    border-radius 12px
    opacity 0
    pointer-events none
    transition standard
    box-shadow 0px 2px 0px var(--thick), 0px 8px 8px -2px var(--thick), 0px 0px 10px -2px var(--thick)

    .transactionDataBodyCopied
      position absolute
      top 0
      left 0
      right 0
      display flex
      justify-content center
      align-items center
      font-size 16px
      letter-spacing 0px
      height 76px

      svg
        margin -3px 0px 0px 10px

    .transactionDataBodyInner
      opacity 0
      top -100px
      position relative
      max-width 100%
      width 100%
      height 100%
      font-size 15px
      text-transform none
      transition standardSlow
      overflow-wrap break-word
      word-wrap break-word
      font-family FiraCode, monospace
      overflow-x hidden
      overlflow-y scroll
      border-radius 6px

  .transactionDataSelected
    position absolute
    left 5px
    right 5px
    top 43px
    height 280px

    .transactionDataHeader
      .transactionDataIndicator
        transform rotate(0deg)

    .transactionDataBody
      opacity 1
      pointer-events auto
      .transactionDataBodyInner
        opacity 1
        top 0px

  .transactionNoData
    background var(--ghostD)
    border-radius 12px
    box-shadow 0px 2px 0px var(--thick), 0px 8px 8px -2px var(--thick), 0px 0px 10px -2px var(--thick)

  .transactionTo
    height 70px
    width 100%
    display flex
    justify-content center
    align-items center
    flex-direction column
    position absolute
    bottom 60px
    right 0
    left 0

    .transactionToAddress
      font-size 12px
      font-family firaCode
      position relative
      height 25px
      width 100%
      -webkit-app-region no-drag

      .transactionToAddressLarge
        position absolute
        top 0
        right 0
        bottom 0
        left 0
        display flex
        justify-content center
        align-items center
        font-size 20px
        opacity 1
        transition standard

        svg
          color var(--outerspace08)
          padding-top 5px
          padding-left 5px
          padding-right 5px

      .transactionToAddressFull
        position absolute
        top 0
        right 0
        bottom 0
        left 0
        display flex
        justify-content center
        align-items center
        font-size 12px
        opacity 0
        transition standard
        font-weight bold

    .transactionToAddress:hover
        .transactionToAddressLarge
          opacity 0
        .transactionToAddressFull
          opacity  1

    .transactionToSub
      font-weight 400
      text-transform uppercase
      font-size 13px
      margin-top 4px
      letter-spacing 1px

  .unknownType
    display flex
    justify-content center
    align-items center
    width 100%
    margin-bottom 60px
    padding 20px

  .requestApprove
    position absolute
    left 0
    right 0
    bottom 0
    height 60px
    border-bottom-left-radius 16px
    border-bottom-right-radius 16px
    font-size 14px
    text-transform uppercase
    -webkit-app-region no-drag
    pointer-events auto
    opacity 1
    transition standardFast
    display flex
    justify-content space-around
    align-items center
    font-weight 400
    border-top 1px solid var(--thick)
    background var(--ghostD)

    .requestDecline
      position relative
      width 50%
      height 100%
      border-right 1px solid var(--thick)
      overflow hidden
      position relative

    .requestDeclineButton
      position absolute
      top 0
      right 0
      bottom 0
      left 0
      animation scalein 3000ms cubic
      transform translateZ(0px)
      display flex
      justify-content center
      align-items center
      -webkit-app-region no-drag
      transition standard
      letter-spacing 2px
      padding-left 2px
      padding-bottom 10px

    .requestDeclineButton:before
      content ''
      position absolute
      margin-left -1px
      height 4px
      border-radius 2px
      width 30px
      background var(--bad06)
      bottom 14px

    .requestDeclineButton:hover:before
      background var(--bad)

    .requestSign
      position relative
      width 50%
      height 100%
      overflow hidden

    .requestSignButton
      position absolute
      top 0
      right 0
      bottom 0
      left 0
      animation scalein 3000ms cubic
      transform translateZ(0px)
      display flex
      justify-content center
      align-items center
      -webkit-app-region no-drag
      transition standard
      letter-spacing 2px
      padding-left 2px
      padding-bottom 10px

    .requestSignButton:before
      content ''
      position absolute
      margin-left -1px
      height 4px
      border-radius 2px
      width 30px
      background var(--good06)
      bottom 14px

    .requestSignButton:hover:before
      background var(--good)

  .requestQuickButton
    animation scalein 800ms cubic !important

  .requestApproveDisabled
    pointer-events none
    opacity 0.2
    .requestDecline
      pointer-events none
    .requestSign
      pointer-events none

.signerRequestsHidden
  left -100%
  right 100%

.monitorConfirms
  position absolute
  top 35px
  left 51px
  right 0
  padding 3px 15px 3px 0px
  display flex
  justify-content space-between
  z-index 1002
  box-sizing border-box

  .txProgressConfirmsItem
    font-size 12px
    font-weight bold
    color var(--thick)
    svg
      max-width 14px
      min-width 14px
      color var(--doubleThick)
      transition standardFast

  .txProgressConfirmsItemActive
    svg
      color var(--good)

  .txProgressConfirmsItemGood
    svg
      color var(--outerspace08)

.monitorIcon
  position absolute
  width 50px
  height 40px
  top 0
  left 0
  box-sizing border-box
  padding 14px 17px

.monitorIconIndicator
  position absolute
  height 4px
  border-radius 2px
  width 100%
  background var(--outerspace08)
  top 43px
  left 14px
  width 24px

.monitorTo
  font-family FiraCode, monospace
  svg
    position relative
    padding 0px 3px
    top -1px

.monitorValue
  font-family FiraCode, monospace

.monitorArrow
  padding-top 1px

.monitorDeploy
  text-transform uppercase
  font-size 13px
  letter-spacing 1px
  font-weight 500

span
  padding 0px 3px 0px 0px

.txNonce
  position absolute
  top 20px
  right 12px
  background var(--ghostD)
  height 20px
  width 48px
  border-radius 12px
  box-shadow 0px 2px 1px var(--thick)
  display flex
  justify-content center
  align-items center
  font-size 12px
  font-weight 600
  background var(--ghostD)
  color var(--outerspace05)
  transition standard
  z-index 9999
  overflow hidden

  .txNonceMarker
    position absolute
    background var(--thick)
    top 7px
    right 7px
    bottom 7px
    width 34px
    border-radius 6px

  .txNonceNumber
    position absolute 
    top -1px
    right 0
    bottom 0px
    width 48px
    text-align center
    opacity 1
    font-weight 400
    font-size 13px
    display flex
    justify-content center 
    align-items center

  .txNonceHidden
    color var(--tripleThick)
    opacity 0

  .txNonceControl
    position absolute 
    top 0px
    right 48px
    bottom 0px
    width 98px
    display flex
    justify-conent space-between

  .txNonceButton
    height 20px
    width 22px
    display flex
    justify-content center 
    align-items center
    font-size 10px
    text-transform uppercase
    border-right 1px solid var(--thick)

  .txNonceButton:hover
    background var(--ghostA)

  .txNonceButtonLower 
    padding 1px 0px 0px 4px

  .txNonceButtonRaise
    padding 0px 0px 1px 0px

  // .txNonceLower
  //   height 100%
  //   width 22px
  //   text-align center
  //   font-size 10px
  //   text-transform uppercase
    
  .txNonceLabel
    height 100%
    width 60px
    text-align center
    font-size 10px
    text-transform uppercase
    border-right 1px solid var(--thick)
    display flex
    justify-content center 
    align-items center

.txNonce:hover
  color var(--outerspace08)
  width 145px
  background var(--ghostC)
  box-shadow 0px 2px 1px var(--thick), -8px 0px 8px var(--ghostE), -8px 0px 8px var(--ghostE)

  .txNonceHidden
    opacity 1

  .txNonceMarker
    opacity 0

.txNonceSet
  transform translate(1px, -9px)

.monitorTop
  position absolute
  z-index 1001
  top 0px
  left 53px
  right 53px
  height 40px
  padding 14px 16px 3px 0px
  font-size 14px
  text-transform none
  transition standardSlow
  overflow-wrap break-word
  word-wrap break-word
  overflow-x hidden
  display flex
  justify-content space-between
  font-weight 400
  color var(--outerspace08)
  box-sizing border-box

.monitorBot
  position absolute
  z-index 1001
  height 35px
  bottom 1px
  left 10px
  right 12px
  font-size 12px
  text-transform none
  transition standardSlow
  overflow-wrap break-word
  word-wrap break-word
  text-transform uppercase
  overflow-x hidden
  display flex
  justify-content center
  align-items center
  font-weight 00
  letter-spacing 1px
  color var(--outerspace08)

  span
    padding 0px 7px 0px 0px

  svg
    padding 0px 3px 0px 3px

.txAugment
  position absolute
  bottom 60px
  left 5px
  height 32px
  right 5px
  font-size 12px
  display flex
  justify-content space-between
  align-items center
  text-transform uppercase  
  letter-spacing 0.2px
  font-weight 500
  border-radius 12px
  background var(--ghostD)
  box-shadow 0px 2px 1px var(--thick), 0px 8px 5px -5px var(--thin), 0px -2px 6px var(--thinner)
  z-index 20000000009999
  

  .txAugmentCancel
    position absolute
    top 0px
    left 0px
    bottom 0px
    border-radius 4px
    border-top-left-radius 16px
    border-bottom-left-radius 16px
    right 72%
    color var(--bad)
    display flex
    justify-content center
    align-items center
    transform translateY(0px)
  
  .txAugmentSpeedUp
    position absolute
    top 0px
    right 0px
    bottom 0px
    border-radius 4px
    border-top-right-radius 16px
    border-bottom-right-radius 16px
    left 72%
    color var(--good)
    display flex
    justify-content center
    align-items center
    transform translateY(0px)

  .txDetails
    position absolute
    top 0px
    right 28%
    left 28%
    border-radius 4px
    bottom 0px
    display flex
    justify-content center
    align-items center
    transform translateY(0px)
    z-index 900000000
  
  .txDetailsHide
    pointer-events none 
    opacity 0

  .txAugmentCancel:hover, .txAugmentSpeedUp:hover, .txDetails:hover
    background var(--ghostD)
    transform scale(1.05)
    box-shadow 0px 4px 5px -2px var(--thick), 0px 25px 25px -5px var(--thick)
  
  // .txAugmentCancel:active, .txAugmentSpeedUp:active, .txDetails:active
  //   transform translateY(0px) scale(1)
  //   box-shadow none

.txAugmentHidden
  .txAugmentCancel
    opacity 0
    transition standard
    background var(--ghostD)
    transform translateY(-25px) scale(1.05)
    box-shadow 0px 4px 5px -3px var(--thick), 0px 35px 20px -5px var(--thick)
  .txAugmentSpeedUp
    opacity 0
    background var(--ghostD)
    transform translateY(-25px) scale(1.05)
    transform translateY(-25px)   
    transition standard
  .txDetails
    background none

.txSuccessHash
  position absolute
  top 60px
  left 5px
  height 32px
  width 199px
  display flex
  justify-content center
  align-items center
  font-weight 500
  transition standardSlow
  opacity 0
  transform translateY(25px)
  font-family Fira Code, monospace
  overflow hidden
  font-size 16px
  padding-top 2px
 
  svg
    color var(--outerspace08)
    padding 4px 5px 2px 5px

.txSuccessHashShow
  opacity 1
  transform translateY(0px)

// .txDetails
//   position absolute
//   top 60px
//   right 5px
//   height 32px
//   width 116px
//   border-radius 12px
//   font-size 12px
//   display flex
//   justify-content center
//   align-items center
//   text-transform uppercase
//   letter-spacing 0.5px
//   margin-left 0.5px
//   font-weight 500
//   background highlight
//   box-shadow 0px 2px 1px var(--thick), 0px 8px 5px -5px var(--thick), 0px -2px 6px var(--thinner)
//   transition standardSlow
//   transition-delay 0.2s
//   opacity 0
//   transform translateY(-25px)

// .txDetails:hover, .txStatus:hover
//   background var(--ghostD)

.txStatus
  position absolute
  top 63px
  left 5px
  right 5px
  bottom 100px
  border-radius 12px
  // box-shadow 0px 2px 1px var(--thick), 0px 8px 5px -5px var(--thin), 0px -2px 6px var(--thinner)
  text-align center
  box-sizing border-box
  // background rgba(255, 255, 255, 0.05)
  z-index 100000
  font-size 14px
  font-weight 400
  text-transform uppercase
  letter-spacing 0.5px
  margin-left 0.5px
  overflow hidden
  transition standard
  display flex
  justify-content center
  align-items center

  .txProgressNoticeHidden
    transition standard
    transform translateY(-25px)
    opacity 0
    pointer-events none

  .txProgressNoticeSuccess
    position absolute
    top 50%
    height 50px
    left 0
    right 0
    z-index 1000005
    margin-top -24px
    display flex
    justify-content center
    align-items center
    flex-direction column
    transition standard

.txStatusError
  top 30px
  bottom 0px

.txProgressSuccess
  position absolute
  bottom 24px
  height 32px
  left 0px
  right 0px
  z-index 1000005
  display flex
  transition standard

  .txProgressSuccessLine
    position absolute 
    top 5px
    left 50%
    width 2px
    bottom 5px
    background var(--tripleThick)

  .txProgressSuccessItem
    position absolute
    display flex
    align-items center
    justify-content center
    box-sizing border-box
    padding-top 1px
    margin 5px 0px

    .txProgressSuccessItemValue
      display flex
      justify-content center
      align-items center
      font-size 14px
      font-weight 600
      font-family FiraCode, monospace
      letter-spacing -0.5px

    .txProgressSuccessItemLabel
      font-size 8.5px
      display flex
      justify-content center
      align-items center
      font-weight 600
      text-transform uppercase
      padding 4px

  .txProgressSuccessItem:last-child
    border-right none

  .txProgressSuccessItemLeft
    left 0
    width 130px
  .txProgressSuccessItemCenter
    right 113px
    left 130px
    border-left 2px solid var(--thick)
  .txProgressSuccessItemRight
    right 0
    width 110px
    border-left 2px solid var(--thick)

.txSummary
  display flex
  justify-content center
  align-items center
  height 100%
  width 100%

.txProgressDetailExpand
  color var(--outerspace05)
  font-size 11px

.txProgressDetailError
  padding 0px 10px

.txStatusCompact
  bottom 60px

.txBar
  position absolute
  z-index 1001
  height 58px
  bottom 0px
  width 331px
  border-bottom-left-radius 16px
  border-bottom-right-radius 16px
  overflow hidden
  transition standard

  .txProgress
    .txProgressBack
      position absolute
      top 0
      right -20px
      bottom 0
      left -20px
      z-index 1001

      .txProgressLine
        position absolute
        top 40px
        left 0
        right 0
        height 2px
        margin-top -1px
        background var(--thick)
        z-index 982

      .txProgressSteps
        position absolute
        top 0
        right 50px
        bottom 0
        left 48px
        z-index 994
        display flex
        .txProgressStep
          position relative
          width calc(100% / 3)
          height 100%

          .txProgressStepIcon
            position absolute
            z-index 996
            left 50%
            top 0
            margin-left -20px
            width 40px
            height 40px
            box-sizing border-box
            transition standard
            transform translateZ(0px)
          .txProgressStepIconHidden
            transform translateY(-50px)
            opacity 0
            transition-delay 500
            transition standardFast


          .txProgressStepMarker
            position absolute
            z-index 996
            left 50%
            top 40px
            height 12px
            width 12px
            background red
            margin-top -6px
            margin-left -6px
            border-radius 12px
            background var(--ghostD)

          .txProgressStepCenter
            position absolute
            z-index 997
            left 50%
            top 40px
            height 6px
            width 6px
            margin-top -3px
            margin-left -3px
            border-radius 3px
            background var(--thick)

          .txProgressStepCenterOn
            background var(--nebula)

    .txProgressFront
      position absolute
      top 0
      right 0px
      bottom 0
      left 0
      z-index 1001
      transition standard

      .txProgressSlide
        position absolute
        top 0
        right 61px
        bottom 0
        left 0
        z-index 1001
        transition standardSlow
        pointer-events none

        .txProgressTail
          position absolute
          top 40px
          left 0
          right 0
          height 2px
          margin-top -1px
          background var(--nebula)
          transition standard

        .txProgressLoading
          position absolute
          z-index 1007
          height 16px
          width 16px
          border-radius 12px 
          overflow hidden
          animation spin 0.7s infinite linear
          position absolute
          z-index 1012
          top 40px
          right -8px
          margin-top -8px

          .txProgressLoadingBox
            position absolute
            z-index 1008
            top 0
            right 0
            width 50%
            height 50%
            background var(--nebula)

          .txProgressLoadingCenter
            position absolute
            z-index 1010
            height calc(100% - 4px)
            width calc(100% - 4px)
            border-radius 18px
            background var(--ghostD)
            top 2px
            left 2px

          .txProgressLoadingDot
            position absolute
            z-index 1011
            height calc(100% - 8px)
            width calc(100% - 8px)
            border-radius 18px
            top 4px
            left 4px
            background var(--nebula)

  .txBarSheen
    position absolute
    z-index 3002
    height 30px
    width 200px
    animation sheen 12s infinite ease
    background linear-gradient(90deg, rgba(247, 251, 255, 0), rgba(237, 241, 255, 0.5), rgba(247, 251, 255, 0))
    background-blend-mode multiply

.txBarSuccess
  .txProgressTail
    transition standardSlow
    background var(--good) !important
  .txProgressLoadingBox
    transition standardSlow
    background var(--good)  !important
  .txProgressLoadingDot
    transition standardSlow
    background var(--good)  !important
  .txProgressStepCenterOn
    transition standardSlow
    background var(--good)  !important

.txBarError
  .txProgressTail
    transition standardSlow
    background var(--bad) !important
  .txProgressLoadingBox
    transition standardSlow
    background var(--bad) !important
  .txProgressLoadingDot
    transition standardSlow
    background var(--bad) !important
  .txProgressStepCenterOn
    transition standardSlow
    background var(--bad) !important


.txBarCondensed
  height 50px
  .txStep
    transform translateY(0px)
    transition standard

.confirmBar
  position absolute
  z-index 1001
  background var(--ghostD)
  height 60px
  width 310px
  bottom 10px
  border-radius 12px
  overflow hidden
  font-size 12px
  font-weight bold
  text-transform uppercase

  .confirmBarStartText
    position absolute
    left 0px
    top 0
    bottom 0
    width 60px
    display flex
    justify-content flex-start
    align-items center
    z-index 2450
    padding-left 19px
    color var(--spacewhite)
    svg
      fill var(--spacewhite)
      color var(--spacewhite)
  .confirmBarEndText
    position absolute
    right 0
    top 0
    bottom 0
    width 80px
    display flex
    justify-content flex-end
    align-items center
    z-index 1150
    padding-right 9px
    svg
      fill var(--outerspace)
      color var(--outerspace)
  .confirmBarStart
    z-index 1204
    position absolute
    left -70px
    width 120px
    height 120px
    transform rotate(45deg)
    top -29px
    border-radius 12px
    background var(--ghostE)
    border-radius 12px
    box-shadow 5px -5px 40px var(--thick)
  .confirmBarSent
    z-index 1202
    left 0px

  .confirmBarStep
    z-index 1000
    position absolute
    width 120px
    height 120px
    background var(--ghostD)
    box-shadow 5px -5px 40px var(--thick)
    transform rotate(45deg)
    top -29px
    margin-left -60px
    border-radius 12px

.confirmBarDropBottom
  box-shadow cardDrop, 0px 2px 0px var(--thick), 0px 8px 8px -2px var(--thick), 0px 0px 10px -2px var(--thick)

.txMonitorBar
  position absolute
  left 10px
  right 10px
  top 10px
  height 30px
  border-radius 15px

.signerRequestSuccess
  .monitorIconIndicator
    background var(--good) !important

.signerRequestMonitor
  .txStatus
    transform translateY(0px)

.signerRequestError
  .monitorIconIndicator
    background var(--bad) !important
  .txProgressConfirmsItem
    svg
      color var(--bad) !important

.signerUnlockRequest
  display flex
  align-items center
  flex-direction column
  justify-content flex-end
  transform translateY(0px)
  height 3px
  transition standard
  font-size 12px
  font-weight 400
  text-transform uppercase
  letter-spacing 3px
  height 110px
  .signerUnlockWrap
    display flex
    align-items center
    flex-direction column
    transform translateY(0px)
    min-height 100px
    height 0
    width 100%
    transition standard
    padding-bottom 10px
  .signerUnlockInput
    height 40px
    width calc(100% - 30px)
    margin 20px 0px 0px 0px
    padding-bottom 0px
    border 0
    outline 0
    background var(--spacewhite)
    font-size 20px
    letter-spacing 6px
    display flex
    justify-content center
    align-items center
    text-align center
    border-radius 20px
    color var(--outerspace08)
    box-shadow cardDrop, 0px 2px 0px var(--thin), 0px 8px 8px -2px var(--thin), 0px 0px 10px -2px var(--thin)
  .signerUnlockSubmit
    height 30px
    background var(--spacewhite)
    font-size 12px
    margin 10px 0px 0px 0px
    padding 0px 40px 0px 44px
    border-radius 15px
    display flex
    justify-content center
    align-items center
    color var(--outerspace08)
    font-weight 600
    box-shadow cardDrop,0px 2px 0px var(--thick), 0px 8px 8px -2px var(--thick), 0px 0px 10px -2px var(--thick)

.simpleJson
  font-size 14px

  .simpleJsonChild
    width 100%
    margin-top 10px

  .simpleJsonKey
    font-size 14px
    font-weight bold
    width 100%
    color var(--outerspace05)
    font-family 'FiraCode', monospace

  .simpleJsonValue
    margin 10px 10px 10px 3px
    border-left 2px solid var(--outerspace03)
    padding-left 10px
    font-size 16px
    width calc(100% - 3px)
    box-sizing border-box
    font-weight 600
    word-wrap break-word
    font-family 'FiraCode', monospace


@import './next'
@import '../TransactionRequest/TxModule/style'
