name: Update application version after release has been published

on:
  push:
    branches:
      - 'update-ga'
  #release:
   # types: [published]

env:
  GH_TOKEN: ${{ secrets.GH_RELEASE_DEPLOYMENT_TOKEN }}

jobs:
  bump_release:
    # if: github.repository == 'frame-labs/frame-canary'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Generate next version
        id: update-version
        run: echo "NEW_VERSION=$(grep -Eo '[[:digit:]].[[:digit:]].[[:digit:]]-canary.[[:digit:]]' package.json | awk -F. '{$NF = $NF + 1;} 1' OFS=.)" >> $GITHUB_OUTPUT
      - name: Test new version
        run: echo ${{ steps.update-version.outputs.NEW_VERSION }}
      - name: Set version
        id: set-version
        run: sed -i "s/[[:digit:]].[[:digit:]].[[:digit:]]-canary.[[:digit:]]/${{ steps.update-version.outputs.NEW_VERSION }}/" package.json
      - name: Test output
        run: cat package.json

      # - name: Commit new version
      #   run: |
      #     git commit -am "CI: bump version to ${{ steps.update-version.outputs.NEW_VERSION }}"
      #     git push https://${{ env.GH_TOKEN }}@github.com/frame-labs/frame-canary
